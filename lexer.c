#include <stdio.h>
#include "lexer.h"
#include "array.h"
int fffTokenize(FILE *input, FffToken **tokenList){
        FffArray lineBuf;
        fffArrayInit(&lineBuf);
        fffArrayDestroy(&lineBuf);
        return 0;
}
